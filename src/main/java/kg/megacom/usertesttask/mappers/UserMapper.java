package kg.megacom.usertesttask.mappers;

import kg.megacom.usertesttask.models.User;
import kg.megacom.usertesttask.models.dto.UserDto;
import lombok.RequiredArgsConstructor;
import org.modelmapper.ModelMapper;
import org.springframework.stereotype.Component;

import java.util.Objects;

@Component
@RequiredArgsConstructor
public class UserMapper {
//    private final ModelMapper modelMapper;
//
//    public User toEntity (UserDto studentDto){
//        return Objects.isNull(userDto) ? null : modelMapper.map(userDto, User.class);
//    }
//    public UserDto toDto(User user){
//        return Objects.isNull(user) ? null : modelMapper.map(user,UserDto.class);
//    }



}
